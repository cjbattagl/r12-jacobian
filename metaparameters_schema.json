{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Jacobian feature RF",
  "technique": "RF Regressor on 4 Singular Values",
  "technique_description": "This is just a mediocre, baseline implementation that trains a bagged RF on 4 singular values from the penultimate weight layer. Directly after we will submit our better classifier that uses Jacobian features.",
  "technique_changes": "Initial trial",
  "commit_id": "1d65177c82f2200f15f1f25958ba8cd859b2fc2f",
  "repo_name": "https://github.com/cjbattagl/r12-jacobian/tree/cyber-pdf-dec2022",
  "required": ["train_random_forest_regressor_param_n_estimators"],
  "additionalProperties": false,
  "type": "object",
  "properties": {
		"train_random_forest_regressor_param_n_estimators": {
			"description": "Number of trees in the forest.",
			"type": "integer",
			"minimum": 1,
			"maximum": 200,
			"suggested_minimum": 75,
			"suggested_maximum": 150
		}
  }
}